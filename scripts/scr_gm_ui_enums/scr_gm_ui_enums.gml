enum UI_FLEX_DIRECTION {
	VERTICAL,
	HORIZONTAL
}

enum UI_EVENT {
	ON_MOUSE_ENTER,
	ON_MOUSE_EXIT,
	ON_MOUSE_PRESSED,
	ON_MOUSE_RELEASED
}

enum IMAGE_FIT {
	SCALED,
	STATIC,
	KEEP_ASPECT
}

enum TEXT_OVERFLOW {
	EXTEND,
	HIDE
}