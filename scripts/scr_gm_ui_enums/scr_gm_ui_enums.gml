enum UI_FLEX_DIRECTION {
	VERTICAL,
	HORIZONTAL
}

enum UI_EVENT {
	ON_MOUSE_ENTER,
	ON_MOUSE_EXIT,
	ON_MOUSE_PRESSED,
	ON_MOUSE_RELEASED,
	ON_DISABLE,
	ON_ENABLE
}

enum UI_IMAGE_FIT {
	SCALED,
	STATIC,
	KEEP_ASPECT
}

enum UI_TEXT_OVERFLOW {
	EXTEND,
	HIDE
}

enum UI_JUSTIFY_CONTENT {
	START,
	CENTER,
	SPACE_BETWEEN,
	SPACE_EVENLY,
	FILL_WIDTH
}